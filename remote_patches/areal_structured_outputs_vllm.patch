*** Begin Patch
*** Update File: areal/experimental/openai/client.py
@@
-        model_request = ModelRequest(
+        request_metadata = dict(metadata if not is_omitted(metadata) else {})
+        if isinstance(extra_body, dict) and extra_body.get("structured_outputs") is not None:
+            request_metadata["structured_outputs"] = extra_body["structured_outputs"]
+
+        model_request = ModelRequest(
             input_ids=prompt_token_ids,
             gconfig=gconfig,
             rid=str(uuid.uuid4()),
-            metadata=metadata if not is_omitted(metadata) else {},
+            metadata=request_metadata,
             tokenizer=self.tokenizer,
             image_data=image_data if has_images else None,
             vision_msg_vllm=([vision_messages_for_vllm] if has_images else None),
*** Update File: areal/engine/vllm_remote.py
@@
         payload = {
             "top_p": gconfig.top_p,
             "top_k": gconfig.top_k,
             "max_tokens": gconfig.max_new_tokens,
@@
             "stream": False,
         }
+        if isinstance(req.metadata, dict) and req.metadata.get("structured_outputs") is not None:
+            payload["structured_outputs"] = req.metadata["structured_outputs"]
 
         if with_lora:
             lora_name = gconfig.lora_name
*** Update File: areal/experimental/inference_service/vllm/bridge.py
@@
         payload: dict[str, Any] = {
             "top_p": gconfig.top_p,
             "top_k": gconfig.top_k,
             "max_tokens": max_new_tokens,
@@
             "stream": False,
         }
+        if isinstance(req.metadata, dict) and req.metadata.get("structured_outputs") is not None:
+            payload["structured_outputs"] = req.metadata["structured_outputs"]
 
         if with_lora:
             lora_name = gconfig.lora_name
*** End Patch
